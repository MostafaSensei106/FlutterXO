enum ListTileGroupType { none, single, top, middle, bottom }

enum GameMode { vsAI, vsFriend }

enum AIDifficulty { easy, medium, hard }

enum GameStatus { playing, draw, xWins, oWins }

enum TimeMode { classic, rapid5, rapid10, rapid15, rapid30, blitz, bullet }
